# Cyrex Vacancies GRPC Proto definitions

These files were provided for the purposes of defining the gRPC communication. These files were used to auto-generate the classes in the `../proto_classes` folder.
